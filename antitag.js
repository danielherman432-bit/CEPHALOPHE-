import config from '../config.js';const userMentionWarnings = new Map();export default {    name: 'antitag',    description: 'Prot猫ge le groupe contre les mentions dans les statuts',    adminOnly: false,    category: 'group',    execute: async (sock, msg, args, sender, isGroup, participant, commands) => {        const prefix = config.config.prefix;                if (!isGroup) {            return await sock.sendMessage(sender, {                text: '鉂� Cette commande fonctionne uniquement dans les groupes'            });        }                const groupMetadata = await sock.groupMetadata(sender);        const isAdmin = groupMetadata.participants            .filter(p => p.admin)            .some(p => p.id === participant);                if (!isAdmin) {            return await sock.sendMessage(sender, {                text: '鉂� Seuls les admins du groupe peuvent utiliser cette commande'            });        }                const groupId = sender;        const action = args[0]?.toLowerCase();                if (!action || (action !== 'on' && action !== 'off')) {            const settings = config.getSettings(groupId);            const status = settings.antiMention ? '鉁� Activ茅' : '鉂� D茅sactiv茅';            return await sock.sendMessage(sender, {                text: `鈺攣鈹佲攣 *ANTI-MENTION* 鈹佲攣鈹�鈹�鈹� 馃搶 *Statut:* ${status}鈹� 鈿狅笍 *Warn max:* 3鈹�鈹� 馃摉 *Commandes:*鈹� ${prefix}antitag on  鈫� Activer鈹� ${prefix}antitag off 鈫� D茅sactiver鈹�鈹� 馃摑 *Description:*鈹� Supprime les mentions du groupe鈹� dans les statuts WhatsApp鈹�鈺扳攣鈹佲攣鈹佲攣鈹佲攣鈹佲攣鈹佲攣鈹佲攣鈹佲攣> ${config.config.botName}`            });        }                if (action === 'on') {            config.updateGroupSetting(groupId, 'antiMention', true);            await sock.sendMessage(sender, {                text: `鉁� *Anti-mention activ茅*\n\nLes mentions du groupe dans les statuts seront supprim茅es. 3 warns = expulsion.`            });        }         else if (action === 'off') {            config.updateGroupSetting(groupId, 'antiMention', false);            if (userMentionWarnings.has(groupId)) {                userMentionWarnings.delete(groupId);            }            await sock.sendMessage(sender, {                text: `鉂� *Anti-mention d茅sactiv茅*`            });        }    },        handleStatus: async (sock, statusMsg, groupJid) => {        const settings = config.getSettings(groupJid);        if (!settings.antiMention) return;                const text = statusMsg.message?.textMessage?.text ||                     statusMsg.message?.conversation || '';                const groupLink = `https://chat.whatsapp.com/`;        const groupId = groupJid.replace('@g.us', '');                if (!text.includes(groupLink) && !text.includes(groupId)) return;                const sender = statusMsg.key?.participant || statusMsg.key?.remoteJid;                const groupMetadata = await sock.groupMetadata(groupJid);        const isAdmin = groupMetadata.participants            .filter(p => p.admin)            .some(p => p.id === sender);                if (isAdmin) return;                if (!userMentionWarnings.has(groupJid)) {            userMentionWarnings.set(groupJid, new Map());        }                const groupWarnings = userMentionWarnings.get(groupJid);        const currentWarns = groupWarnings.get(sender) || 0;        const newWarns = currentWarns + 1;                try {            await sock.sendMessage(groupJid, {                delete: statusMsg.key            });        } catch (err) {}                if (newWarns >= 3) {            await sock.sendMessage(groupJid, {                text: `馃毃 *EXPULSION*\n\n@${sender.split('@')[0]} a 茅t茅 expuls茅 pour avoir mentionn茅 le groupe 3 fois dans ses statuts.`,                mentions: [sender]            });                        await sock.groupParticipantsUpdate(groupJid, [sender], 'remove');            groupWarnings.delete(sender);        } else {            groupWarnings.set(sender, newWarns);            await sock.sendMessage(groupJid, {                text: `鈿狅笍 *MENTION INTERDITE*\n\n@${sender.split('@')[0]}, tu ne peux pas mentionner le groupe dans tes statuts.\n\n鈿狅笍 Avertissement ${newWarns}/3`,                mentions: [sender]            });        }    }};